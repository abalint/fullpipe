# API keys — copy to .env (gitignored) and fill in what you use.

# Punctuation restore (engine/punctuation.py). Required for sub-less sources
# whose ASR output lacks sentence punctuation.
OPENAI_API_KEY=

# ElevenLabs Scribe V2 — the default online ASR (word-level timestamps).
ELEVENLABS_API_KEY=
